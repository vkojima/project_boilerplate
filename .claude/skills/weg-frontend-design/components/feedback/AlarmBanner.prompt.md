```jsx
<AlarmBanner severity="critical" message="Parada não programada — Linha 3, Posto 2" timestamp="14:32:07" onAcknowledge={() => {}} />
```
Never relies on animation alone to draw attention — color + icon + persistent placement do the work; a brief pulse may accompany arrival only.
