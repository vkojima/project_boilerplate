```jsx
<Toast status="warning" title="Temperatura elevada" message="Posto 2 acima do limite configurado." onClose={() => {}} />
```
Appears/dismisses with the fast fade in Motion tokens — never a bounce/slide.
