Icon-only square button for toolbars, table row actions, and nav-adjacent controls. Always pass `label` — it's the only accessible name.

```jsx
<IconButton icon="settings" label="Configurações" />
<IconButton icon="bell" label="Alarmes" active variant="outline" />
```
