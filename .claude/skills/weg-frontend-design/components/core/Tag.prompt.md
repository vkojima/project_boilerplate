Plain neutral chip — for filters, applied-filter lists, and device/line association tags. Unlike `Badge`, `Tag` carries no status color/meaning.

```jsx
<Tag>Linha 3</Tag>
<Tag onRemove={() => {}}>Turno A</Tag>
```
