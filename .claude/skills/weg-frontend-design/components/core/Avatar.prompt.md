Small circular initials badge — used in the top nav account menu and Configuration's user list. No photo/image support (this system has no photo library); falls back to a generic user icon if `name` is empty.

```jsx
<Avatar name="Carlos Souza" />
```
