Labeled text/number field, used heavily in Configuration for thresholds and setpoints (pair with `unit`).

```jsx
<Input label="Temperatura máxima" value="90" unit="°C" />
<Input label="Nome da linha" error="Campo obrigatório" />
```
